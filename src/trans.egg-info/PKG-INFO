Metadata-Version: 2.4
Name: trans
Version: 0.1.0
Summary: Offline Chinese-English command-line translator
Requires-Python: >=3.11
Description-Content-Type: text/markdown
Provides-Extra: dev
Requires-Dist: pytest<10,>=9; extra == "dev"
Provides-Extra: runtime
Requires-Dist: argostranslate<2,>=1.11; extra == "runtime"

# trans

适用于 `WSL / Linux` 的离线中英命令行翻译。

## 功能

- 默认交互模式
- 支持 `中文 -> 英文`、`英文 -> 中文`
- 默认自动识别中英
- 支持 `-f`、`-t` 强制指定方向
- 首次下载模型，之后离线运行

## 安装

```bash
python -m venv .venv
source .venv/bin/activate
pip install --no-build-isolation -e '.[runtime]'
```

如果你也要跑测试：

```bash
pip install --no-build-isolation -e '.[dev]'
```

## 说明

`Argos Translate` 的运行时依赖会比较大，首次安装可能会拉取 `torch` 等包，耗时和体积都明显高于普通 CLI 工具。

## 初始化模型

```bash
trans --install-models
```

这一步会下载：

- `zh -> en`
- `en -> zh`

下载完成后，就可以离线使用。

## 用法

进入交互模式：

```bash
trans
```

一次性翻译：

```bash
trans "你好，今天怎么样？"
trans "How are you today?"
```

强制方向：

```bash
trans -f zh -t en
trans -f en -t zh "hello"
```

## 交互命令

- `:q` 退出
- `:swap` 切换 `zh -> en` 和 `en -> zh`
- `:from zh` 指定源语言
- `:to en` 指定目标语言
- `:auto` 回到自动识别

## 不安装 console script 时的直接运行方式

```bash
PYTHONPATH=src python -m trans_cli.cli
```
